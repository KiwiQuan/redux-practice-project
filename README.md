# Redux Practice Project

This is a practice project to learn how to use Redux.

It's an API dashboard that displays the data from jsonplaceholder API.

This project is built with React, Redux, and Shadcn.
It uses Redux Toolkit for state management. and the createAsyncThunk for fetching data from the API.
It uses Shadcn for the UI.
